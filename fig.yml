db:
    image: postgres
redis:
    image: redis
elasticsearch:
    image: dockerfile/elasticsearch
web:
    build: .
    volumes:
        - .:/code
    ports:
        - "8000:8000"
    links:
        - db
        - elasticsearch
        - redis
#worker:
#    build: .
#    command: celery -A sandbox worker -l info
#    volumes:
#        - .:/code
#    links:
#        - db
#        - elasticsearch
#        - redis
flower:
    build: stack/flower
    links:
        - redis
    ports:
        - "5555:5555"
